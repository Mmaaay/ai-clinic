#!/bin/sh

echo "🔍 Running pre-commit validation..."

# Get list of staged files
STAGED_FILES=$(git diff --cached --name-only)


# Check if any UI files are staged
UI_CHANGED=$(echo "$STAGED_FILES" | grep -E "^ai-clinic-ui/" || true)

# Validate UI service if UI files changed
if [ -n "$UI_CHANGED" ]; then
  echo ""
  echo "📦 UI files changed - validating environment..."
  if [ -f "ai-clinic-ui/.env" ]; then
    cd ai-clinic-ui
    npx tsx scripts/validate-env.ts
    UI_STATUS=$?
    cd ..
    if [ $UI_STATUS -ne 0 ]; then
      echo "❌ UI environment validation failed!"
      exit 1
    fi
  else
    echo "⚠️  Skipping UI validation (no .env found)"
  fi
fi

# Run lint-staged for UI if UI files changed
if [ -n "$UI_CHANGED" ]; then
  echo ""
  echo "🔧 Running lint-staged for UI..."
  cd ai-clinic-ui
  npx lint-staged
  LINT_STATUS=$?
  cd ..
  if [ $LINT_STATUS -ne 0 ]; then
    echo "❌ Lint-staged failed!"
    exit 1
  fi
fi

echo ""
echo "✅ All pre-commit checks passed!"
exit 0
